{
	"name": "react-native-sqlite-mobx",
	"version": "1.0.0",
	"description": "Library with amazing functions for sqlite and mobx",
	"main": "dist/index.js",
	"author": "Carlos Roberto",
	"license": "MIT",
	"scripts": {
		"postinstall": "tsc --declaration index.ts --declarationDir typescript --outDir dist --lib esnext --target esnext"
	},
	"dependencies": {
		"mobx": "^5.10.1",
		"react-native-sqlite-storage": "^3.3.10"
	},
	"devDependencies": {
		"@types/react-native-sqlite-storage": "^3.3.1",
		"typescript": "^3.5.2"
	}
}
